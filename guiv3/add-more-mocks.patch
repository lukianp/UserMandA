--- a/src/renderer/components/organisms/VirtualizedDataGrid.test.tsx
+++ b/src/renderer/components/organisms/VirtualizedDataGrid.test.tsx
@@ -20,6 +20,14 @@ import { ColDef } from 'ag-grid-community';
 import { VirtualizedDataGrid } from './VirtualizedDataGrid';
 
 // Mock AG Grid
+jest.mock('ag-grid-enterprise', () => ({}));
+
+// Mock lucide-react icons
+jest.mock('lucide-react', () => ({
+  Download: () => <span>Download Icon</span>,
+  Printer: () => <span>Printer Icon</span>,
+  Eye: () => <span>Eye Icon</span>,
+  EyeOff: () => <span>EyeOff Icon</span>,
+  Filter: () => <span>Filter Icon</span>,
+  Loader2: () => <span>Loader Icon</span>,
+}));
+
 jest.mock('ag-grid-react', () => ({
   AgGridReact: ({ rowData, columnDefs, onRowClicked, onSelectionChanged, loading }: any) => (
     <div data-cy="ag-grid-mock">
