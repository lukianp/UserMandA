<Window x:Class="MandADiscoverySuite.Dialogs.ColumnVisibilityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Column Visibility" Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#FF1A202C"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4299E1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF3182CE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF2C5282"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#FF2D3748"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4A5568"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox Style -->
        <Style x:Key="ModernCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFE2E8F0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder" 
                                    Grid.Column="0"
                                    Width="18" Height="18" 
                                    Background="#FF2D3748" 
                                    BorderBrush="#FF4A5568" 
                                    BorderThickness="1" 
                                    CornerRadius="3" 
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark" 
                                      Data="M 0 2 L 2 4 L 6 0" 
                                      Stroke="#FF4299E1" 
                                      StrokeThickness="2" 
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                            HorizontalAlignment="Left" 
                                            VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="#FF1A365D"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF4299E1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF4299E1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="#FF1A202C" BorderBrush="#FF4A5568" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <TextBlock Text="{Binding WindowTitle}" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Foreground="#FFE2E8F0" 
                           HorizontalAlignment="Left"/>
                <Button Content="✕" 
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Top"
                        Background="Transparent" 
                        Foreground="#FFA0AEC0" 
                        BorderThickness="0" 
                        FontSize="16" 
                        Padding="8,4" 
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Description -->
            <TextBlock Grid.Row="1" 
                       Text="Select which columns to show or hide in the data grid." 
                       Foreground="#FFA0AEC0" 
                       FontSize="13" 
                       Margin="0,8,0,16"/>

            <!-- Column List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="250">
                <ItemsControl x:Name="ColumnList" ItemsSource="{Binding Columns}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding DisplayName}" 
                                      IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                      Style="{StaticResource ModernCheckBoxStyle}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Quick Actions -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,8">
                <Button Content="Show All" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Margin="0,0,8,0" 
                        Click="ShowAllButton_Click"/>
                <Button Content="Hide All" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Margin="0,0,8,0" 
                        Click="HideAllButton_Click"/>
                <Button Content="Reset" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Click="ResetButton_Click"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Apply" 
                        Style="{StaticResource ModernButtonStyle}" 
                        Margin="0,0,8,0" 
                        Click="ApplyButton_Click"/>
                <Button Content="Cancel" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>