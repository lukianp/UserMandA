<Window x:Class="MandADiscoverySuite.Dialogs.FilterPresetManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Filter Preset Manager"
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Preset Card Style -->
            <Style x:Key="PresetCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Category Button Style -->
            <Style x:Key="CategoryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Margin" Value="0,0,8,8"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,0,0,1" 
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Filter Preset Manager" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="Manage and organize your saved filter configurations" 
                               FontSize="13" 
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Import" 
                            Command="{Binding ImportPresetCommand}"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            Margin="0,0,8,0"/>
                    <Button Content="Export Selected" 
                            Command="{Binding ExportSelectedCommand}"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            IsEnabled="{Binding HasSelection}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar - Categories and Filters -->
            <Border Grid.Column="0" 
                    Background="{DynamicResource SurfaceBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="0,0,1,0"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Search -->
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,16"/>

                        <!-- Categories -->
                        <TextBlock Text="Categories" 
                                   FontWeight="Medium" 
                                   Margin="0,0,0,8"/>
                        
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                            Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource CategoryButtonStyle}"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            Margin="0,0,0,4"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Separator Margin="0,16"/>

                        <!-- Sort Options -->
                        <TextBlock Text="Sort By" 
                                   FontWeight="Medium" 
                                   Margin="0,0,0,8"/>
                        
                        <ComboBox SelectedItem="{Binding SortOption}" 
                                  ItemsSource="{Binding SortOptions}"
                                  DisplayMemberPath="DisplayName"
                                  Margin="0,0,0,8"/>

                        <CheckBox Content="Show Favorites Only"
                                  IsChecked="{Binding ShowFavoritesOnly}"
                                  Margin="0,0,0,8"/>
                        
                        <CheckBox Content="Show Recent Only"
                                  IsChecked="{Binding ShowRecentOnly}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Main Content - Preset List -->
            <ScrollViewer Grid.Column="1" 
                          VerticalScrollBarVisibility="Auto" 
                          Padding="16">
                <ItemsControl ItemsSource="{Binding FilteredPresets}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource PresetCardStyle}"
                                    MouseLeftButtonDown="PresetCard_MouseLeftButtonDown">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Header -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="⭐" 
                                                   FontSize="14"
                                                   Margin="0,0,8,0"
                                                   Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold"/>
                                        <Border Background="{DynamicResource AccentBrush}" 
                                                CornerRadius="2" 
                                                Padding="4,2" 
                                                Margin="8,0,0,0">
                                            <TextBlock Text="{Binding Category}" 
                                                       FontSize="10" 
                                                       Foreground="White"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Row="0" Grid.Column="1" 
                                                Orientation="Horizontal">
                                        <Button Content="Load" 
                                                Command="{Binding DataContext.LoadPresetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Load Preset"
                                                Style="{StaticResource ThemedButtonStyle}"
                                                FontSize="10"
                                                Padding="6,2"
                                                Margin="0,0,4,0"/>
                                        <Button Content="⭐" 
                                                Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Toggle Favorite"
                                                Style="{StaticResource ThemedSecondaryButtonStyle}"
                                                FontSize="10"
                                                Padding="4,2"
                                                Margin="0,0,4,0"/>
                                        <Button Content="Del" 
                                                Command="{Binding DataContext.DeletePresetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Delete"
                                                Style="{StaticResource ThemedDangerButtonStyle}"
                                                FontSize="10"
                                                Padding="6,2"/>
                                    </StackPanel>

                                    <!-- Description -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                               Text="{Binding Description}" 
                                               FontSize="12" 
                                               Foreground="{DynamicResource MutedForegroundBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,4,0,0"
                                               Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>

                                    <!-- Tags and Info -->
                                    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                                Orientation="Horizontal" 
                                                Margin="0,8,0,0">
                                        
                                        <!-- Tags -->
                                        <ItemsControl ItemsSource="{Binding Tags}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{DynamicResource MutedBrush}" 
                                                            CornerRadius="2" 
                                                            Padding="4,2" 
                                                            Margin="0,0,4,4">
                                                        <TextBlock Text="{Binding}" 
                                                                   FontSize="10" 
                                                                   Foreground="{DynamicResource ForegroundBrush}"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Info -->
                                        <StackPanel Orientation="Horizontal" 
                                                    HorizontalAlignment="Right" 
                                                    Margin="8,0,0,0">
                                            <TextBlock Text="{Binding UsageCount, StringFormat={}Used {0} times}" 
                                                       FontSize="10" 
                                                       Foreground="{DynamicResource MutedForegroundBrush}"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock Text="•" 
                                                       FontSize="10" 
                                                       Foreground="{DynamicResource MutedForegroundBrush}"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding LastModified, StringFormat={}Modified {0:MMM d}}" 
                                                       FontSize="10" 
                                                       Foreground="{DynamicResource MutedForegroundBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" 
                Background="{DynamicResource SurfaceBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,1,0,0" 
                Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="{Binding FilteredPresets.Count, StringFormat={}{0} presets}" 
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource MutedForegroundBrush}"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Close" 
                            IsCancel="True"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            Margin="0,0,8,0"/>
                    <Button Content="Load Selected" 
                            Command="{Binding LoadSelectedCommand}"
                            IsDefault="True"
                            Style="{StaticResource ThemedButtonStyle}"
                            IsEnabled="{Binding HasSelection}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>