<Window x:Class="MandADiscoverySuite.CreateProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Company Profile" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="NoResize"
        Style="{DynamicResource ThemedWindowStyle}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/ThemeStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Create New Company Profile" 
                   Style="{StaticResource ThemedHeaderTextStyle}"
                   FontSize="18" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Company Name:" 
                   Style="{StaticResource ThemedTextBlockStyle}"
                   Margin="0,0,0,8"/>
        
        <StackPanel Grid.Row="2">
            <TextBox x:Name="ProfileNameTextBox" 
                     Style="{StaticResource ThemedTextBoxStyle}"
                     Padding="10,8"
                     KeyDown="ProfileNameTextBox_KeyDown"
                     TextChanged="ProfileNameTextBox_TextChanged"/>
            <TextBlock x:Name="ProfileNameValidationText" 
                       Style="{StaticResource ThemedValidationErrorTextStyle}"
                       Margin="0,4,0,16"
                       Visibility="Collapsed"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True"
                    Style="{StaticResource ThemedSecondaryButtonStyle}"
                    Padding="20,8" Margin="0,0,10,0"/>
            
            <Button x:Name="CreateButton" Content="Create" Click="Create_Click" IsDefault="True"
                    Style="{StaticResource ThemedButtonStyle}"
                    Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>