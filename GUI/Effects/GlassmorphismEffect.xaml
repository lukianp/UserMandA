<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Glassmorphism Background Brushes -->
    
    <!-- Light Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismLightBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#40FFFFFF" Offset="0"/>
            <GradientStop Color="#20FFFFFF" Offset="0.5"/>
            <GradientStop Color="#30FFFFFF" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Dark Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismDarkBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#30000000" Offset="0"/>
            <GradientStop Color="#40000000" Offset="0.5"/>
            <GradientStop Color="#35000000" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Accent Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismAccentBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#300078D4" Offset="0"/>
            <GradientStop Color="#200078D4" Offset="0.5"/>
            <GradientStop Color="#250078D4" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Success Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismSuccessBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#3048BB78" Offset="0"/>
            <GradientStop Color="#2048BB78" Offset="0.5"/>
            <GradientStop Color="#2548BB78" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Warning Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismWarningBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#30F6AD55" Offset="0"/>
            <GradientStop Color="#20F6AD55" Offset="0.5"/>
            <GradientStop Color="#25F6AD55" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Error Glassmorphism Background -->
    <LinearGradientBrush x:Key="GlassmorphismErrorBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#30E53E3E" Offset="0"/>
            <GradientStop Color="#20E53E3E" Offset="0.5"/>
            <GradientStop Color="#25E53E3E" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Glassmorphism Border Brushes -->
    <LinearGradientBrush x:Key="GlassmorphismBorderBrush" StartPoint="0,0" EndPoint="1,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#60FFFFFF" Offset="0"/>
            <GradientStop Color="#30FFFFFF" Offset="0.5"/>
            <GradientStop Color="#40FFFFFF" Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!-- Glassmorphism Card Style -->
    <Style x:Key="GlassmorphismCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassmorphismLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassmorphismBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" 
                                Opacity="0.15" 
                                ShadowDepth="4" 
                                BlurRadius="20"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        
        <Style.Triggers>
            <!-- Theme-aware glassmorphism -->
            <DataTrigger Binding="{Binding Source={x:Static Application.Current}, Path=(FrameworkElement.ActualTheme)}" Value="Dark">
                <Setter Property="Background" Value="{StaticResource GlassmorphismDarkBrush}"/>
            </DataTrigger>
            
            <!-- Hover effect -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" 
                                        Opacity="0.25" 
                                        ShadowDepth="6" 
                                        BlurRadius="25"/>
                    </Setter.Value>
                </Setter>
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                           To="1.02" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                           To="1.02" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                           To="1" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                           To="1" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Glassmorphism Button Style -->
    <Style x:Key="GlassmorphismButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GlassmorphismLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassmorphismBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="BorderElement"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        
                        <Border.Effect>
                            <DropShadowEffect Color="Black" 
                                            Opacity="0.1" 
                                            ShadowDepth="2" 
                                            BlurRadius="8"/>
                        </Border.Effect>
                        
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        TextBlock.Foreground="{TemplateBinding Foreground}"
                                        RecognizesAccessKey="True"/>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderElement" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" 
                                                    Opacity="0.2" 
                                                    ShadowDepth="4" 
                                                    BlurRadius="12"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="BorderElement" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#50FFFFFF" Offset="0"/>
                                        <GradientStop Color="#30FFFFFF" Offset="0.5"/>
                                        <GradientStop Color="#40FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderElement" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#30FFFFFF" Offset="0"/>
                                        <GradientStop Color="#10FFFFFF" Offset="0.5"/>
                                        <GradientStop Color="#20FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="BorderElement" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="BorderElement" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        </Trigger>
                        
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        
                        <!-- Theme-aware background -->
                        <DataTrigger Binding="{Binding Source={x:Static Application.Current}, Path=(FrameworkElement.ActualTheme)}" Value="Dark">
                            <Setter Property="Background" Value="{StaticResource GlassmorphismDarkBrush}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Glassmorphism Panel Style -->
    <Style x:Key="GlassmorphismPanelStyle" TargetType="StackPanel">
        <Setter Property="Background" Value="{StaticResource GlassmorphismLightBrush}"/>
        <Setter Property="Margin" Value="8"/>
        
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="BorderBrush" Value="{StaticResource GlassmorphismBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" 
                                        Opacity="0.1" 
                                        ShadowDepth="3" 
                                        BlurRadius="10"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static Application.Current}, Path=(FrameworkElement.ActualTheme)}" Value="Dark">
                <Setter Property="Background" Value="{StaticResource GlassmorphismDarkBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Glassmorphism TextBox Style -->
    <Style x:Key="GlassmorphismTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource GlassmorphismLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassmorphismBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="BorderElement"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        
                        <Border.Effect>
                            <DropShadowEffect Color="Black" 
                                            Opacity="0.05" 
                                            ShadowDepth="1" 
                                            BlurRadius="6"/>
                        </Border.Effect>
                        
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                            
                            <!-- Placeholder Text -->
                            <TextBlock x:Name="PlaceholderText"
                                      Text="{TemplateBinding Tag}"
                                      Foreground="{DynamicResource MutedForegroundBrush}"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      IsHitTestVisible="False"
                                      Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderElement" Property="BorderBrush">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#80FFFFFF" Offset="0"/>
                                        <GradientStop Color="#50FFFFFF" Offset="0.5"/>
                                        <GradientStop Color="#60FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="BorderElement" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="Black" 
                                                    Opacity="0.1" 
                                                    ShadowDepth="2" 
                                                    BlurRadius="8"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="BorderElement" Property="BorderBrush" 
                                    Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="BorderElement" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="BorderElement" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{DynamicResource AccentColor}" 
                                                    Opacity="0.3" 
                                                    ShadowDepth="0" 
                                                    BlurRadius="10"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value=""/>
                                <Condition Property="IsFocused" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                        
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        
                        <!-- Theme-aware background -->
                        <DataTrigger Binding="{Binding Source={x:Static Application.Current}, Path=(FrameworkElement.ActualTheme)}" Value="Dark">
                            <Setter Property="Background" Value="{StaticResource GlassmorphismDarkBrush}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Glassmorphism Notification Style -->
    <Style x:Key="GlassmorphismNotificationStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource GlassmorphismLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassmorphismBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" 
                                Opacity="0.2" 
                                ShadowDepth="4" 
                                BlurRadius="15"/>
            </Setter.Value>
        </Setter>
        
        <!-- Entrance Animation -->
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="20"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Opacity" Value="0"/>
        
        <Style.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                       To="1" Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                       To="0" Duration="0:0:0.4">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            
            <DataTrigger Binding="{Binding Source={x:Static Application.Current}, Path=(FrameworkElement.ActualTheme)}" Value="Dark">
                <Setter Property="Background" Value="{StaticResource GlassmorphismDarkBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Glassmorphism Modal Backdrop Style -->
    <Style x:Key="GlassmorphismBackdropStyle" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#30000000" Offset="0"/>
                    <GradientStop Color="#50000000" Offset="0.5"/>
                    <GradientStop Color="#40000000" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <BlurEffect Radius="20"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>