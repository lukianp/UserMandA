<UserControl x:Class="MandADiscoverySuite.Controls.GlobalSearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <Border Background="White" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="3">
            <Grid>
                <!-- Search Box -->
                <TextBox x:Name="SearchTextBox"
                        Height="30"
                        FontSize="14"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="8,6"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        KeyDown="SearchTextBox_KeyDown"
                        GotFocus="SearchTextBox_GotFocus"
                        LostFocus="SearchTextBox_LostFocus" />
                
                <!-- Search Icon -->
                <TextBlock Text="🔍" 
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0"
                          Foreground="#666666"
                          IsHitTestVisible="False"/>
            </Grid>
        </Border>
        
        <!-- Results Popup -->
        <Popup x:Name="ResultsPopup"
               IsOpen="{Binding ShowResults}"
               PlacementTarget="{Binding ElementName=SearchTextBox}"
               Placement="Bottom"
               StaysOpen="False">
            <Border Background="White" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="1"
                    MinWidth="300"
                    MaxHeight="250"
                    Effect="{DynamicResource DropShadowEffect}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        
                        <!-- Search History -->
                        <ItemsControl ItemsSource="{Binding SearchHistory}"
                                     Visibility="{Binding ShowSearchHistory, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent"
                                           MouseLeftButtonDown="History_MouseLeftButtonDown"
                                           MouseEnter="History_MouseEnter">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F0F0F0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid Margin="8,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- History Icon -->
                                            <TextBlock Grid.Column="0" 
                                                      Text="🕒" 
                                                      FontSize="14"
                                                      Margin="0,0,8,0"
                                                      VerticalAlignment="Center"
                                                      Foreground="#999999"/>
                                            
                                            <!-- Search Term -->
                                            <TextBlock Grid.Column="1" 
                                                      Text="{Binding}" 
                                                      FontSize="12"
                                                      VerticalAlignment="Center"
                                                      Foreground="#666666"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- Filter Suggestions -->
                        <ItemsControl ItemsSource="{Binding FilterSuggestions}"
                                     Visibility="{Binding FilterSuggestions.Count, Converter={StaticResource CountToVisibilityConverter}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F5F5F5" 
                                           Margin="2"
                                           Padding="8,4"
                                           CornerRadius="2">
                                        <TextBlock Text="{Binding Text}" 
                                                  FontSize="12"
                                                  Foreground="#666666"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <!-- Search Results -->
                        <ItemsControl ItemsSource="{Binding FilteredResults}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent"
                                           MouseLeftButtonDown="Result_MouseLeftButtonDown"
                                           MouseEnter="Result_MouseEnter">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#E6F3FF"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid Margin="8,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Type Icon -->
                                            <TextBlock Grid.Column="0" 
                                                      Text="{Binding Icon}" 
                                                      FontSize="16"
                                                      Margin="0,0,8,0"
                                                      VerticalAlignment="Center"/>
                                            
                                            <!-- Title and Subtitle -->
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Title}" 
                                                          FontWeight="Bold"
                                                          FontSize="14"/>
                                                <TextBlock Text="{Binding Subtitle}" 
                                                          FontSize="12"
                                                          Foreground="#666666"
                                                          Visibility="{Binding Subtitle, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            </StackPanel>
                                            
                                            <!-- Score -->
                                            <TextBlock Grid.Column="2" 
                                                      Text="{Binding Score, StringFormat={}{0:P0}}" 
                                                      FontSize="10"
                                                      Foreground="#999999"
                                                      VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Popup>

        <!-- Search History Popup -->
        <Popup x:Name="HistoryPopup"
               IsOpen="{Binding ShowSearchHistory}"
               PlacementTarget="{Binding ElementName=SearchTextBox}"
               Placement="Bottom"
               StaysOpen="False">
            <Border Background="White" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="1"
                    MinWidth="300"
                    MaxHeight="200"
                    Effect="{DynamicResource DropShadowEffect}">
                <StackPanel>
                    <!-- History Header -->
                    <Border Background="#F8F8F8" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
                        <Grid Margin="8,4">
                            <TextBlock Text="Recent Searches" FontSize="12" FontWeight="Bold" Foreground="#666666" HorizontalAlignment="Left"/>
                            <Button Content="Clear" 
                                   Command="{Binding ClearHistoryCommand}"
                                   FontSize="10" 
                                   Padding="4,2"
                                   HorizontalAlignment="Right"
                                   Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                                   Background="Transparent"
                                   BorderThickness="0"
                                   Foreground="#888888"/>
                        </Grid>
                    </Border>
                    
                    <!-- History Items -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="160">
                        <ItemsControl ItemsSource="{Binding SearchHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent"
                                           MouseLeftButtonDown="History_MouseLeftButtonDown"
                                           MouseEnter="History_MouseEnter">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F0F0F0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid Margin="8,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- History Icon -->
                                            <TextBlock Grid.Column="0" 
                                                      Text="🕒" 
                                                      FontSize="14"
                                                      Margin="0,0,8,0"
                                                      VerticalAlignment="Center"
                                                      Foreground="#999999"/>
                                            
                                            <!-- Search Term -->
                                            <TextBlock Grid.Column="1" 
                                                      Text="{Binding}" 
                                                      FontSize="13"
                                                      VerticalAlignment="Center"
                                                      Foreground="#333333"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <!-- No History Message -->
                    <TextBlock Text="No recent searches" 
                              FontSize="12" 
                              Foreground="#999999" 
                              Margin="8,12"
                              HorizontalAlignment="Center"
                              Visibility="{Binding SearchHistory.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Inverse}"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>