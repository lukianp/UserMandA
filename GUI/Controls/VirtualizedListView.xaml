<UserControl x:Class="MandADiscoverySuite.Controls.VirtualizedListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Virtualized List Item Style -->
            <Style x:Key="VirtualizedListItemStyle" TargetType="ListBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Margin" Value="0,0,0,1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Loading Item Style -->
            <Style x:Key="LoadingItemStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Height" Value="40"/>
            </Style>

            <!-- Performance Info Style -->
            <Style x:Key="PerformanceInfoStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0,1,0,0"/>
                <Setter Property="Padding" Value="8,4"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Controls -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search and Filter -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox Width="200"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ThemedTextBoxStyle}"
                             ToolTip="Search items..."
                             Margin="0,0,8,0"/>
                    
                    <ComboBox Width="120"
                              ItemsSource="{Binding FilterOptions}"
                              SelectedItem="{Binding SelectedFilter}"
                              Style="{StaticResource ThemedComboBoxStyle}"
                              Margin="0,0,8,0"/>
                    
                    <Button Content="🔄"
                            ToolTip="Refresh Data"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            Padding="6"/>
                </StackPanel>

                <!-- Virtualization Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding ItemsPerPage, StringFormat='Page Size: {0}'}"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               Margin="0,0,8,0"/>
                    
                    <ComboBox Width="80"
                              ItemsSource="{Binding PageSizeOptions}"
                              SelectedItem="{Binding ItemsPerPage}"
                              Style="{StaticResource ThemedComboBoxStyle}"
                              Margin="0,0,8,0"/>
                    
                    <Button Content="⚙️"
                            ToolTip="Virtualization Settings"
                            Command="{Binding ShowSettingsCommand}"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            Padding="6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Virtualized List -->
        <Grid Grid.Row="1">
            <!-- Main List -->
            <ListBox x:Name="VirtualizedListBox"
                     ItemsSource="{Binding VirtualizedItems}"
                     SelectedItem="{Binding SelectedItem}"
                     ItemContainerStyle="{StaticResource VirtualizedListItemStyle}"
                     ScrollViewer.CanContentScroll="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     VirtualizingPanel.ScrollUnit="Item">
                
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Item Icon -->
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Icon}"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>

                            <!-- Item Content -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                           FontSize="13"
                                           FontWeight="Medium"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Subtitle}"
                                           FontSize="11"
                                           Foreground="{DynamicResource MutedForegroundBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding Subtitle, Converter={StaticResource StringToVisibilityConverter}}"/>
                            </StackPanel>

                            <!-- Item Status -->
                            <Border Grid.Column="2"
                                    Background="{Binding StatusColor}"
                                    CornerRadius="10"
                                    Padding="6,2"
                                    Margin="0,0,8,0"
                                    Visibility="{Binding Status, Converter={StaticResource StringToVisibilityConverter}}">
                                <TextBlock Text="{Binding Status}"
                                           FontSize="9"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>

                            <!-- Item Actions -->
                            <StackPanel Grid.Column="3" 
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                <Button Content="👁️"
                                        ToolTip="View Details"
                                        Command="{Binding DataContext.ViewItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ThemedSecondaryButtonStyle}"
                                        Padding="4"
                                        FontSize="10"
                                        Margin="0,0,4,0"/>
                                <Button Content="✏️"
                                        ToolTip="Edit Item"
                                        Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ThemedSecondaryButtonStyle}"
                                        Padding="4"
                                        FontSize="10"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Loading Overlay -->
            <Border Background="{DynamicResource BackgroundBrush}"
                    Opacity="0.9"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True"
                                 Width="200"
                                 Height="4"
                                 Style="{StaticResource ThemedProgressBarStyle}"
                                 Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding LoadingMessage}"
                               FontSize="12"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Empty State -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                <TextBlock Text="No items found"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
                <TextBlock Text="Try adjusting your search or filter criteria"
                           FontSize="12"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Footer with Performance Info -->
        <Border Grid.Row="2" Style="{StaticResource PerformanceInfoStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Item Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="{Binding TotalItems, StringFormat='Total: {0:N0} items'}"
                               FontSize="11"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0"/>
                    
                    <TextBlock Text="{Binding DisplayedItems, StringFormat='Showing: {0:N0} items'}"
                               FontSize="11"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0"/>
                    
                    <TextBlock Text="{Binding SelectedItemsCount, StringFormat='Selected: {0:N0}'}"
                               FontSize="11"
                               Foreground="{DynamicResource AccentBrush}"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasSelectedItems, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Performance Info -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding RenderTime, StringFormat='Render: {0}ms'}"
                               FontSize="10"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    
                    <TextBlock Text="{Binding MemoryUsage, StringFormat='Memory: {0}'}"
                               FontSize="10"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    
                    <Button Content="📊"
                            ToolTip="Performance Details"
                            Command="{Binding ShowPerformanceCommand}"
                            Style="{StaticResource ThemedSecondaryButtonStyle}"
                            Padding="4"
                            FontSize="9"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>