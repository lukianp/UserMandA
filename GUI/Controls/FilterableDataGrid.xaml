<UserControl x:Class="MandADiscoverySuite.Controls.FilterableDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MandADiscoverySuite.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Filter Bar -->
        <Border Grid.Row="0" 
                Background="#FF1A202C" 
                BorderBrush="#FF2D3748" 
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Search and Quick Filters -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <!-- Global Search -->
                    <Border Background="#FF2D3748" 
                            BorderBrush="#FF4A5568" 
                            BorderThickness="1" 
                            CornerRadius="4" 
                            Padding="8,6"
                            Margin="0,0,12,0">
                        <Grid Width="250">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                       Text="🔍"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       FontSize="14"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"/>
                            
                            <TextBox Grid.Column="1" 
                                     Text="{Binding GlobalSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent" 
                                     Foreground="#FFE2E8F0"
                                     BorderThickness="0"
                                     FontSize="13"
                                     VerticalAlignment="Center"/>
                            
                            <Button Grid.Column="2"
                                    Content="✕"
                                    Command="{Binding ClearSearchCommand}"
                                    Background="Transparent"
                                    Foreground="{StaticResource SecondaryTextBrush}"
                                    BorderThickness="0"
                                    FontSize="12"
                                    Width="20" Height="20"
                                    Visibility="{Binding HasSearchText, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>
                    </Border>
                    
                    <!-- Active Filters Indicator -->
                    <Border Background="#FF667eea" 
                            CornerRadius="12" 
                            Padding="8,4"
                            Margin="0,0,8,0"
                            Visibility="{Binding HasActiveFilters, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ActiveFilterCount}" 
                                       Foreground="White" 
                                       FontSize="11" 
                                       FontWeight="Bold"
                                       Margin="0,0,4,0"/>
                            <TextBlock Text="filters active" 
                                       Foreground="White" 
                                       FontSize="11"
                                       Margin="0,0,6,0"/>
                            <Button Content="✕"
                                    Command="{Binding FilterViewModel.ClearFilterCommand}"
                                    Background="Transparent" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    FontSize="10"
                                    Width="16" Height="16"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Results Count -->
                    <TextBlock Text="{Binding ItemCount, StringFormat='{}{0:N0} items'}"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                </StackPanel>
                
                <!-- Filter Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔽 Export" 
                            Command="{Binding ExportDataCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,8,0"
                            Padding="8,4"
                            FontSize="12"/>
                    
                    <Button Content="🔧 Columns" 
                            Command="{Binding ShowColumnConfigCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,8,0"
                            Padding="8,4"
                            FontSize="12"/>
                    
                    <Button Content="🔍 Filters" 
                            Command="{Binding FilterViewModel.ToggleFilterPanelCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="{Binding FilterViewModel.IsFilterPanelVisible, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#FF667eea|#FF2D3748'}"
                            Padding="8,4"
                            FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Data Grid -->
            <DataGrid x:Name="MainDataGrid"
                      Grid.Column="0" 
                      ItemsSource="{Binding FilteredItems}" 
                      SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      IsReadOnly="{Binding IsReadOnly}"
                      Background="Transparent"
                      BorderThickness="0"
                      RowBackground="#FF0F1419"
                      AlternatingRowBackground="#FF1A202C"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#FF374151"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow">
                
                <!-- Column Header Style -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF2D3748"/>
                        <Setter Property="Foreground" Value="#FFE2E8F0"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderBrush" Value="#FF4A5568"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <ContentPresenter Grid.Column="0" 
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="Center"/>
                                            
                                            <!-- Column Filter Button -->
                                            <Button Grid.Column="1"
                                                    Content="⚙"
                                                    Background="Transparent"
                                                    Foreground="{StaticResource SecondaryTextBrush}"
                                                    BorderThickness="0"
                                                    FontSize="10"
                                                    Width="16" Height="16"
                                                    Margin="4,0,0,0"
                                                    ToolTip="Column Filter"
                                                    Opacity="0.6">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                
                <!-- Cell Style -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFE2E8F0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF4A5568"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#FF2D3748"/>
                            </MultiTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                
                <!-- Row Style -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1A202C"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF374151"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
            
            <!-- Filter Panel -->
            <views:AdvancedFilterView Grid.Column="1" 
                                      DataContext="{Binding FilterViewModel}"/>
            
            <!-- Empty State -->
            <StackPanel Grid.Column="0" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.3"/>
                <TextBlock Text="No data to display"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           FontSize="16"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Try adjusting your filters or search criteria"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>
            
            <!-- Loading Overlay -->
            <Border Grid.ColumnSpan="2" 
                    Background="#80000000" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                                 Width="200" 
                                 Height="4" 
                                 Background="Transparent" 
                                 Foreground="#FF667eea" 
                                 Margin="0,0,0,16"/>
                    <TextBlock Text="Loading data..." 
                               Foreground="#FFE2E8F0" 
                               FontSize="14" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>