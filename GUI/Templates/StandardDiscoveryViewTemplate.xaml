<UserControl x:Class="MandADiscoverySuite.GUI.Templates.StandardDiscoveryViewTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Header Section -->
        <TextBlock Grid.Row="0" Text="Discovery View Header" Style="{StaticResource HeaderTextStyle}" FontSize="20"/>
        <!-- Placeholder: Replace with customized header content in each view -->

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Placeholder for Summary Card 1 -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Card 1 Title" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="Value: 0" FontSize="24" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            <!-- Placeholder for Summary Card 2 -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Card 2 Title" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="Value: 0" FontSize="24" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            <!-- Placeholder for Summary Card 3 -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Card 3 Title" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="Value: 0" FontSize="24" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            <!-- Placeholder for Summary Card 4 -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Card 4 Title" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="Value: 0" FontSize="24" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Refresh" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Clear Filters" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <!-- Placeholder: Add more buttons as needed in each view -->
        </ToolBar>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid Placeholder -->
            <Border Grid.Column="0" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1">
                <TextBlock Text="DataGrid Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
                <!-- Placeholder: Replace with actual DataGrid in each view -->
            </Border>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel Placeholder -->
            <Border Grid.Column="2" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1">
                <TextBlock Text="Details Panel Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
                <!-- Placeholder: Replace with details content in each view -->
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Footer/Empty State Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
            <!-- Placeholder: Replace with footer or empty state content in each view -->
        </Border>
    </Grid>
</UserControl>