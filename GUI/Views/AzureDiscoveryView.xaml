<UserControl x:Class="MandADiscoverySuite.Views.AzureDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="AzureDiscoveryControl"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="{DynamicResource SurfaceBrush}">

        <UserControl.Resources>
         <ResourceDictionary>
             <ResourceDictionary.MergedDictionaries>
                 <ResourceDictionary Source="../Resources/Converters.xaml" />
                 <ResourceDictionary Source="../Resources/DataGridTheme.xaml" />
                 <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
                 <ResourceDictionary Source="../Themes/Colors.xaml" />
             </ResourceDictionary.MergedDictionaries>
         </ResourceDictionary>
     </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Azure Discovery" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="Discover and manage Azure resources and configurations"
                       FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Header Warnings Banner -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding HeaderWarnings}"
                      Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}"
                      Margin="20,0,20,10">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                            CornerRadius="4" Margin="0,0,0,8" Padding="8">
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Error Banner -->
        <Border Grid.Row="2" Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                CornerRadius="4" Margin="20,0,20,10" Padding="8"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}">
            <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
        </Border>

        <!-- Summary Cards -->
        <WrapPanel Grid.Row="3" Margin="20,0,20,20" Orientation="Horizontal">
            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,16,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Total Subscriptions" Style="{StaticResource MetricLabelStyle}" FontWeight="SemiBold" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalSubscriptions}" Style="{StaticResource MetricNumberStyle}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,16,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Total Resource Groups" Style="{StaticResource MetricLabelStyle}" FontWeight="SemiBold" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalResourceGroups}" Style="{StaticResource MetricNumberStyle}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource SecondaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,16,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Total Virtual Machines" Style="{StaticResource MetricLabelStyle}" FontWeight="SemiBold" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalVirtualMachines}" Style="{StaticResource MetricNumberStyle}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource TertiaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}" Width="250">
                <StackPanel>
                    <TextBlock Text="Last Discovery" Style="{StaticResource MetricLabelStyle}" FontWeight="SemiBold" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding LastDiscoveryTime, StringFormat='{}{0:g}'}" Style="{StaticResource MetricNumberStyle}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- Action Buttons and Data Area -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,0,20,10">
                <Button Content="?? Run Discovery"
                        Command="{Binding RunDiscoveryCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,12,0"/>

                <Button Content="?? Refresh Data"
                        Command="{Binding RefreshDataCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,12,0"/>

                <Button Content="?? Export"
                        Command="{Binding ExportCommand}"
                        Style="{StaticResource OutlineButtonStyle}"/>

                <Button Content="?? Show Logs"
                        Command="{Binding ViewLogsCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="12,0,0,0"/>
            </StackPanel>

            <!-- Data Grid and Details Panel -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="600"/>
                    <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="Auto" MinWidth="300"/>
                </Grid.ColumnDefinitions>

                <!-- Loading Spinner -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                        <TextBlock Text="{Binding ProcessingMessage, FallbackValue='Processing...'}" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Data Grid -->
                <DataGrid Grid.Column="0"
                          ItemsSource="{Binding SelectedResults}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Margin="20,0,20,20"
                          SelectedItem="{Binding SelectedItem}"
                          Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}" VirtualizingStackPanel.IsVirtualizing="True">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Resource Name"
                                            Binding="{Binding resourcename}"
                                            Width="200"/>

                        <DataGridTextColumn Header="Type"
                                            Binding="{Binding resourcetype}"
                                            Width="150"/>

                        <DataGridTextColumn Header="Region"
                                            Binding="{Binding region}"
                                            Width="120"/>

                        <DataGridTextColumn Header="Resource Group"
                                            Binding="{Binding resourcegroup}"
                                            Width="150"/>

                        <DataGridTextColumn Header="Status"
                                            Binding="{Binding status}"
                                            Width="100"/>

                        <DataGridTextColumn Header="Creation Date"
                                            Binding="{Binding createddatetime, StringFormat=\{0:g\}}"
                                            Width="120"/>

                        <DataGridTextColumn Header="Tags"
                                            Binding="{Binding tags}"
                                            Width="200"/>

                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="View Details"
                                            Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>

                <!-- No Data Message -->
                <TextBlock Grid.Column="0"
                           Text="No Azure discovery data available"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource MutedForegroundBrush}"
                           Visibility="{Binding HasResults, Converter={StaticResource InverseBoolToVisibility}}"/>

                <!-- Separator -->
                <GridSplitter Grid.Column="1" Width="2" Background="{DynamicResource BorderBrush}"
                             HorizontalAlignment="Center" VerticalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

                <!-- Details Panel -->
                <Border Grid.Column="2" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Margin="0,0,20,20" CornerRadius="4"
                        Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="16">
                            <TextBlock Text="Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            <ItemsControl ItemsSource="{Binding SelectedItemDetails}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,10">
                                            <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Foreground="{DynamicResource MutedForegroundBrush}"/>
                                            <TextBlock Text="{Binding Value}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
