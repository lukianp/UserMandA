<UserControl x:Class="MandADiscoverySuite.Views.ApplicationDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Temporarily removed resource dictionaries to avoid initialization issues -->
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🔧" FontSize="32" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="Application Discovery" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalApplications, StringFormat=' ({0} applications)'}" FontSize="16" Foreground="Gray" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>

        <!-- Summary Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Total Applications -->
            <Border Background="#E6F3FF" BorderBrush="#0066CC" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Total Applications" FontWeight="Bold" FontSize="14" Foreground="#0066CC"/>
                    <TextBlock Text="📱" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalApplications}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0066CC"/>
                </StackPanel>
            </Border>
            <!-- Web Applications -->
            <Border Background="#F0F9FF" BorderBrush="#0891B2" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Web Applications" FontWeight="Bold" FontSize="14" Foreground="#0891B2"/>
                    <TextBlock Text="🌐" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding WebApplications}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0891B2"/>
                </StackPanel>
            </Border>
            <!-- Windows Apps -->
            <Border Background="#E0F2FE" BorderBrush="#0277BD" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Windows Apps" FontWeight="Bold" FontSize="14" Foreground="#0277BD"/>
                    <TextBlock Text="🖥️" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding WindowsApplications}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0277BD"/>
                </StackPanel>
            </Border>
            <!-- Services -->
            <Border Background="#E8F5E8" BorderBrush="#2E7D32" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Services" FontWeight="Bold" FontSize="14" Foreground="#2E7D32"/>
                    <TextBlock Text="⚙️" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding Services}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#2E7D32"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Toolbar -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Run Discovery" Command="{Binding RunDiscoveryCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Refresh Data" Command="{Binding RefreshDataCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Padding="12,6"/>
        </ToolBar>

        <!-- Main Content Grid -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid -->
            <DataGrid Grid.Column="0" ItemsSource="{Binding SelectedResults}" AutoGenerateColumns="False" CanUserAddRows="False"
                      Margin="0,0,0,20" SelectedItem="{Binding SelectedItem}" Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}" VirtualizingStackPanel.IsVirtualizing="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Application Name" Binding="{Binding name}" Width="200"/>
                    <DataGridTextColumn Header="Version" Binding="{Binding version}" Width="100"/>
                    <DataGridTextColumn Header="Publisher" Binding="{Binding publisher}" Width="150"/>
                    <DataGridTextColumn Header="Install Date" Binding="{Binding installdate, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Size (MB)" Binding="{Binding size}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel -->
            <Border Grid.Column="2" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                        <TextBlock Text="Application Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" Foreground="#4A5568"/>
                        <TextBlock Text="Name:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.name}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Version:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.version}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Publisher:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.publisher}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Install Date:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.installdate, StringFormat=d}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Size:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.size, StringFormat=\{0:N1\} MB}" FontSize="14" Margin="0,2,0,16" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Application Discovery - Discover and analyze installed applications across your infrastructure" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
        </Border>
    </Grid>
</UserControl>