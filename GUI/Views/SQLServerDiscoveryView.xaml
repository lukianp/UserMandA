<UserControl x:Class="MandADiscoverySuite.Views.SQLServerDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Templates/BaseDiscoveryViewTemplate.xaml" />
                <ResourceDictionary Source="../Resources/Converters.xaml" />
                <ResourceDictionary Source="../Resources/DataGridTheme.xaml" />
                <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
                <ResourceDictionary Source="../Themes/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section with Icon, Title, and Total Records -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Image Source="{Binding Icon}" Width="32" Height="32" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding DisplayName}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalDatabases, StringFormat=' ({0} records)'}" FontSize="16" Foreground="Gray" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>

        <!-- Summary Cards Region -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- SQL Servers Card -->
            <Border Background="#E6F3FF" BorderBrush="#0066CC" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="SQL Servers" FontWeight="Bold" FontSize="14" Foreground="#0066CC"/>
                    <TextBlock Text="???" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalInstances}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0066CC"/>
                </StackPanel>
            </Border>
            <!-- Databases Card -->
            <Border Background="#F0F9FF" BorderBrush="#0891B2" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Databases" FontWeight="Bold" FontSize="14" Foreground="#0891B2"/>
                    <TextBlock Text="???" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalDatabases}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0891B2"/>
                </StackPanel>
            </Border>
            <!-- Storage (GB) Card -->
            <Border Background="#E0F2FE" BorderBrush="#0277BD" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Storage (GB)" FontWeight="Bold" FontSize="14" Foreground="#0277BD"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding StorageGB, StringFormat=N1}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0277BD"/>
                </StackPanel>
            </Border>
            <!-- Large DBs Card -->
            <Border Background="#E8F5E8" BorderBrush="#2E7D32" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Large DBs" FontWeight="Bold" FontSize="14" Foreground="#2E7D32"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding LargeDBs}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#2E7D32"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Toolbar Region -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Run Discovery" Command="{Binding RunDiscoveryCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Refresh Data" Command="{Binding RefreshDataCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Padding="12,6"/>
        </ToolBar>

        <!-- Main Content Grid -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid -->
            <DataGrid Grid.Column="0" ItemsSource="{Binding SelectedResults}" AutoGenerateColumns="False" CanUserAddRows="False"
                      Margin="0,0,0,20" SelectedItem="{Binding SelectedItem}" Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Instance Name" Binding="{Binding instancename}" Width="200"/>
                    <DataGridTextColumn Header="Version" Binding="{Binding version}" Width="120"/>
                    <DataGridTextColumn Header="Edition" Binding="{Binding edition}" Width="120"/>
                    <DataGridTextColumn Header="Databases" Binding="{Binding databasename}" Width="180"/>
                    <DataGridTextColumn Header="Last Backup" Binding="{Binding lastbackup, StringFormat=d}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel -->
            <Border Grid.Column="2" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                        <TextBlock Text="Database Information" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" Foreground="#4A5568"/>
                        <TextBlock Text="Instance:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.instancename}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Database:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.databasename}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Version:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.version}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Edition:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.edition}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Size (MB):" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.size}" FontSize="14" Margin="0,2,0,16" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Backup Status" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="#4A5568"/>
                        <TextBlock Text="Last Backup:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.lastbackup, StringFormat=d}" FontSize="14" Margin="0,2,0,12" TextWrapping="Wrap" Foreground="#E2E8F0"/>
                        <TextBlock Text="Backup Status:" FontSize="12" FontWeight="SemiBold" Foreground="#718096"/>
                        <TextBlock Text="{Binding SelectedItem.backupstatus}" FontSize="14" Margin="0,2,0,0" TextWrapping="Wrap" Foreground="#68D391"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer Region -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="SQL Server Discovery - Database and backup status monitoring" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
        </Border>
    </Grid>
</UserControl>
