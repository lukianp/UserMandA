<UserControl x:Class="MandADiscoverySuite.Views.SQLServerDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="{StaticResource SurfaceBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/ThemeStyles.xaml"/>
                <ResourceDictionary Source="/Themes/Colors.xaml"/>
                <ResourceDictionary Source="/Themes/HighContrastTheme.xaml"/>
                <ResourceDictionary Source="/Resources/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="SQL Server Discovery"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource ForegroundBrush}"/>
            <TextBlock Text="Discover and analyze SQL Server databases and instances"
                       FontSize="12"
                       Foreground="{StaticResource SecondaryForegroundBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Header Warnings Banner -->
        <ItemsControl Grid.Row="1"
                      ItemsSource="{Binding HeaderWarnings}"
                      Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}"
                      Margin="20,0,20,10">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource ErrorBrush}"
                            Opacity="0.33"
                            BorderBrush="{StaticResource ErrorBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Margin="0,0,0,8"
                            Padding="8">
                        <TextBlock Text="{Binding}"
                                   TextWrapping="Wrap"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Error Banner -->
        <Border Grid.Row="2"
                Background="{StaticResource ErrorBrush}"
                Opacity="0.33"
                BorderBrush="{StaticResource ErrorBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Margin="20,0,20,10"
                Padding="8"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}">
            <TextBlock Text="{Binding ErrorMessage}"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource PrimaryTextBrush}"/>
        </Border>

        <!-- Summary Cards -->
        <WrapPanel Grid.Row="3"
                   Margin="20,0,20,20"
                   Orientation="Horizontal">
            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,0,16,0"
                    Padding="16"
                    Width="200">
                <StackPanel>
                    <TextBlock Text="Total Instances"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalInstances}"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,0,16,0"
                    Padding="16"
                    Width="200">
                <StackPanel>
                    <TextBlock Text="Total Databases"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalDatabases}"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,0,16,0"
                    Padding="16"
                    Width="200">
                <StackPanel>
                    <TextBlock Text="Total Tables"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalTables}"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    Width="250">
                <StackPanel>
                    <TextBlock Text="Last Discovery"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding LastDiscoveryTime, StringFormat='{}{0:g}'}"
                               FontSize="14"
                               FontWeight="Bold"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- Action Buttons and Data Area -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="20,0,20,10">
                <Button Content="🔍 Run Discovery"
                        Command="{Binding RunDiscoveryCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,12,0"
                        Foreground="{StaticResource PrimaryTextBrush}"
                        Background="{StaticResource AccentBrush}"/>

                <Button Content="🔄 Refresh Data"
                        Command="{Binding RefreshDataCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,12,0"
                        Foreground="{StaticResource SecondaryTextBrush}"/>

                <Button Content="📤 Export"
                        Command="{Binding ExportCommand}"
                        Style="{StaticResource OutlineButtonStyle}"
                        Margin="0,0,12,0"
                        Foreground="{StaticResource SecondaryTextBrush}"/>

                <Button Content="📋 View Logs"
                        Command="{Binding ViewLogsCommand}"
                        Style="{StaticResource GhostButtonStyle}"
                        Margin="0,0,0,0"
                        Foreground="{StaticResource MutedForegroundBrush}"/>
            </StackPanel>

            <!-- Data Grid and Details Panel -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="600"/>
                    <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="Auto" MinWidth="300"/>
                </Grid.ColumnDefinitions>

                <!-- Loading Spinner -->
                <Border Background="{StaticResource SurfaceBrush}"
                        Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                        <TextBlock Text="{Binding ProcessingMessage, FallbackValue='Processing...'}"
                                   Margin="0,10,0,0"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Data Grid -->
                <DataGrid Grid.Column="0"
                          ItemsSource="{Binding SelectedResults}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Margin="20,0,20,20"
                          SelectedItem="{Binding SelectedItem}"
                          Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}"
                          Foreground="{StaticResource PrimaryTextBrush}"
                          Background="{StaticResource BackgroundBrush}">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Instance Name"
                                            Binding="{Binding instancename}"
                                            Width="200"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Database Name"
                                            Binding="{Binding databasename}"
                                            Width="180"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Server"
                                            Binding="{Binding servername}"
                                            Width="150"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Version"
                                            Binding="{Binding version}"
                                            Width="100"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Status"
                                            Binding="{Binding status}"
                                            Width="80"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Size (MB)"
                                            Binding="{Binding size, StringFormat=N0}"
                                            Width="100"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                        <DataGridTextColumn Header="Last Modified"
                                            Binding="{Binding lastmodified, StringFormat=d}"
                                            Width="100"
                                            Foreground="{StaticResource PrimaryTextBrush}"/>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource SelectedBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>

                <!-- No Data Message -->
                <TextBlock Grid.Column="0"
                           Text="No SQL Server data available"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{StaticResource MutedForegroundBrush}"
                           Visibility="{Binding HasResults, Converter={StaticResource InverseBoolToVisibility}}"/>

                <!-- Separator -->
                <GridSplitter Grid.Column="1"
                              Width="2"
                              Background="{StaticResource BorderBrush}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              ResizeBehavior="PreviousAndNext"/>

                <!-- Details Panel -->
                <Border Grid.Column="2"
                        Background="{StaticResource ElevatedBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        Margin="0,0,20,20"
                        CornerRadius="4"
                        Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="16">
                            <TextBlock Text="SQL Server Details"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,16"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Instance Name:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.instancename}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Database Name:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.databasename}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Server:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.servername}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Version:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.version}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Status:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.status}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource SuccessBrush}"/>

                            <TextBlock Text="Size (MB):"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.size}"
                                       FontSize="14"
                                       Margin="0,2,0,12"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>

                            <TextBlock Text="Description:"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedItem.description}"
                                       FontSize="14"
                                       Margin="0,2,0,0"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource SecondaryTextBrush}"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>