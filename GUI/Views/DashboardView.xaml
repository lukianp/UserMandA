<UserControl x:Class="MandADiscoverySuite.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#FF0F1419">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Dashboard Card Style -->
        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF1A202C"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#FF2D3748"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
        
        <!-- Stat Number Style -->
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF4299E1"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        
        <!-- Stat Label Style -->
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FFA0AEC0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,32">
                <TextBlock Text="📊 Discovery Dashboard" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="#FFF7FAFC" 
                          Margin="0,0,0,8"/>
                <TextBlock Text="Overview of discovered IT infrastructure and assets" 
                          FontSize="16" 
                          Foreground="#FFA0AEC0"/>
            </StackPanel>
            
            <!-- Loading Indicator -->
            <Border Grid.Row="1" 
                    Background="#FF2D3748" 
                    CornerRadius="8" 
                    Padding="16" 
                    Margin="0,0,0,24"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="🔄" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Loading dashboard statistics..." 
                              FontSize="14" 
                              Foreground="#FFE2E8F0" 
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- Statistics Grid -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Top Row Statistics -->
                <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,16">
                    
                    <!-- Users Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalUsers}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="Users" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Computers Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="💻" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalComputers}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="Computers" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Applications Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="📱" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalApplications}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="Applications" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                </UniformGrid>
                
                <!-- Bottom Row Statistics -->
                <UniformGrid Grid.Row="1" Columns="3">
                    
                    <!-- Groups Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🔐" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalGroups}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="Groups" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- File Servers Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="📁" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalFileServers}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="File Servers" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Databases Card -->
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🗄️" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalDatabases}" Style="{StaticResource StatNumberStyle}"/>
                            <TextBlock Text="Databases" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                    </Border>
                    
                </UniformGrid>
                
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>