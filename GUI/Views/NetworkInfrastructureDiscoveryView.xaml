<UserControl x:Class="MandADiscoverySuite.Views.NetworkInfrastructureDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Converters.xaml" />
                <ResourceDictionary Source="../Resources/DataGridTheme.xaml" />
                <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
                <ResourceDictionary Source="../Themes/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#2D3748"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>
            <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
                <Setter Property="MinWidth" Value="150"/>
                <Setter Property="Margin" Value="8,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
                <Setter Property="MinWidth" Value="200"/>
                <Setter Property="Margin" Value="8,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                <Setter Property="Padding" Value="8,6"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Network Infrastructure Discovery" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="Discover and manage network devices and infrastructure"
                       FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,4,0,0"/>

            <!-- Header Warnings Banner -->
            <ItemsControl ItemsSource="{Binding HeaderWarnings}"
                          Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}"
                          Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                                CornerRadius="4" Margin="0,0,0,8" Padding="8">
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Error Banner -->
            <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                    CornerRadius="4" Margin="0,0,0,0" Padding="8"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}">
                <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
            </Border>
        </StackPanel>

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Total Devices -->
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Text="?? Total Devices" Style="{StaticResource MetricLabelStyle}"/>
                    <TextBlock Text="{Binding TotalDevices}" Style="{StaticResource MetricNumberStyle}" Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>
            </Border>
            <!-- Switches -->
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Text="?? Switches" Style="{StaticResource MetricLabelStyle}"/>
                    <TextBlock Text="{Binding TotalSwitches}" Style="{StaticResource MetricNumberStyle}" Foreground="{DynamicResource SecondaryBrush}"/>
                </StackPanel>
            </Border>
            <!-- Routers -->
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Text="??? Routers" Style="{StaticResource MetricLabelStyle}"/>
                    <TextBlock Text="{Binding TotalRouters}" Style="{StaticResource MetricNumberStyle}" Foreground="{DynamicResource TertiaryBrush}"/>
                </StackPanel>
            </Border>
            <!-- Firewalls -->
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Text="?? Firewalls" Style="{StaticResource MetricLabelStyle}"/>
                    <TextBlock Text="{Binding TotalFirewalls}" Style="{StaticResource MetricNumberStyle}" Foreground="{DynamicResource QuaternaryBrush}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="?? Run Discovery" Command="{Binding RunDiscoveryCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="?? Refresh" Command="{Binding RefreshDataCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="?? Export" Command="{Binding ExportCommand}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="?? Show Logs" Command="{Binding ViewLogsCommand}" Padding="12,6" Margin="0,0,8,0"/>
        </ToolBar>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,4,0">
                <Grid>
                    <!-- Loading Spinner -->
                    <Border Background="{DynamicResource SurfaceBrush}"
                            Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}" CornerRadius="4">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                            <TextBlock Text="{Binding ProcessingMessage, FallbackValue='Processing...'}" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Data Grid -->
                    <DataGrid ItemsSource="{Binding SelectedResults}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              Margin="8"
                              SelectedItem="{Binding SelectedItem}"
                              Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}"
                              Background="Transparent" BorderThickness="0">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Device Name" Binding="{Binding device_name}" Width="180"/>
                            <DataGridTextColumn Header="IP Address" Binding="{Binding ip_address}" Width="140"/>
                            <DataGridTextColumn Header="Manufacturer" Binding="{Binding manufacturer}" Width="120"/>
                            <DataGridTextColumn Header="Model" Binding="{Binding model}" Width="120"/>
                            <!-- View Details Button -->
                            <DataGridTemplateColumn Header="Actions" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="View Details"
                                                FontSize="11"
                                                Padding="8,4"
                                                Background="#10B981"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="4"
                                                                        BorderThickness="0">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#059669"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        </DataGrid.Columns>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>

                    <!-- No Data Message -->
                    <TextBlock Text="No Network Infrastructure data available"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="16"
                               Foreground="{DynamicResource MutedForegroundBrush}"
                               Visibility="{Binding HasResults, Converter={StaticResource InverseBoolToVisibility}}"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel -->
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="4,0,0,0"
                    Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Device Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Port Information:" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                        <ItemsControl ItemsSource="{Binding PortDetails}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="{Binding PortName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding PortStatus}" Foreground="{DynamicResource MutedForegroundBrush}" Margin="8,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="VLAN Information:" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,8,0,4"/>
                        <ItemsControl ItemsSource="{Binding VlanDetails}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="{Binding VlanId}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding VlanName}" Foreground="{DynamicResource MutedForegroundBrush}" Margin="8,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding SelectedItemDetails}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,8,0,4">
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding Value}" Foreground="{DynamicResource MutedForegroundBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="{DynamicResource SurfaceBrush}" Padding="16" Margin="0,16,0,0" CornerRadius="4">
            <TextBlock Text="Network Infrastructure Discovery - Standardized Layout" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource MutedForegroundBrush}"/>
        </Border>
    </Grid>
</UserControl>
