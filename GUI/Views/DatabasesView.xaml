<UserControl x:Class="MandADiscoverySuite.Views.DatabasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/GUI/Resources/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Database Servers" FontSize="24" FontWeight="Bold" 
                       Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="Discover and analyze SQL Server instances, databases, and configurations" 
                       FontSize="14" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="15" Margin="0,0,10,0" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="SQL Servers" FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding ServersCount}" FontSize="20" FontWeight="Bold" 
                               Foreground="{DynamicResource PrimaryBrush}"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="15" Margin="5,0" BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Databases" FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding DatabasesCount}" FontSize="20" FontWeight="Bold" 
                               Foreground="{DynamicResource SecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="15" Margin="5,0" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Total Size (GB)" FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalSizeGB}" FontSize="20" FontWeight="Bold" 
                               Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="3" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="15" Margin="10,0,0,0" BorderBrush="#FFE74C3C" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Critical DBs" FontSize="12" Foreground="{DynamicResource MutedForegroundBrush}"/>
                    <TextBlock Text="{Binding CriticalDatabasesCount}" FontSize="20" FontWeight="Bold" 
                               Foreground="#FFE74C3C"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Database Servers Grid -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="20" Margin="0,0,10,0" BorderBrush="{DynamicResource MutedForegroundBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="SQL Server Instances" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10"/>
                    
                    <!-- Search/Filter Bar -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="300" Height="32" VerticalContentAlignment="Center"
                                 Background="{DynamicResource BackgroundBrush}"
                                 BorderBrush="{DynamicResource MutedForegroundBrush}"/>
                        <Button Content="Search" Margin="10,0,0,0" Padding="15,5"
                                Command="{Binding SearchCommand}"/>
                        <ComboBox Width="150" Margin="10,0,0,0" SelectedItem="{Binding SelectedEngine}"
                                  ItemsSource="{Binding EngineFilter}"/>
                    </StackPanel>
                    
                    <!-- Loading/Content States -->
                    <Grid Grid.Row="2">
                        <!-- Loading State -->
                        <StackPanel Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="200" Height="6" Margin="0,0,0,10"/>
                            <TextBlock Text="Loading database servers..." FontSize="14" 
                                       Foreground="{DynamicResource MutedForegroundBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Data Grid -->
                        <DataGrid ItemsSource="{Binding DatabaseServers}" SelectedItem="{Binding SelectedServer}"
                                  Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                                  Background="{DynamicResource BackgroundBrush}"
                                  BorderBrush="{DynamicResource MutedForegroundBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" Width="120"/>
                                <DataGridTextColumn Header="Instance" Binding="{Binding InstanceName}" Width="120"/>
                                <DataGridTextColumn Header="Database" Binding="{Binding DatabaseName}" Width="150"/>
                                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100"/>
                                <DataGridTextColumn Header="Edition" Binding="{Binding Edition}" Width="120"/>
                                <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="100"/>
                                <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="80"/>
                                <DataGridTextColumn Header="Last Backup" Binding="{Binding LastBackup}" Width="100"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <!-- No Data State -->
                        <StackPanel Visibility="{Binding HasNoData, Converter={StaticResource BoolToVisibilityConverter}}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🗄️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="No database servers found" FontSize="16" FontWeight="SemiBold"
                                       Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Run SQL Server discovery to populate data" FontSize="14"
                                       Foreground="{DynamicResource MutedForegroundBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
            
            <!-- Details Panel -->
            <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" 
                    Padding="20" BorderBrush="{DynamicResource MutedForegroundBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Instance Details" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,15"/>
                    
                    <StackPanel Grid.Row="1" Visibility="{Binding HasSelectedServer, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="{Binding SelectedServer.InstanceName}" FontSize="18" FontWeight="Bold" 
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Version: " FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedServer.Version}" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Edition: " FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedServer.Edition}" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Status: " FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedServer.Status}" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Databases: " FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            <TextBlock Text="{Binding SelectedServer.DatabaseCount}" Foreground="{DynamicResource MutedForegroundBrush}"/>
                        </StackPanel>
                        
                        <Button Content="View Databases" Margin="0,15,0,0" Padding="15,8"
                                Command="{Binding ViewDatabasesCommand}"/>
                        <Button Content="View Configuration" Margin="0,10,0,0" Padding="15,8"
                                Command="{Binding ViewConfigurationCommand}"/>
                        <Button Content="Security Analysis" Margin="0,10,0,0" Padding="15,8"
                                Command="{Binding ViewSecurityCommand}"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="1" Text="Select a database server to view details" 
                               FontSize="14" Foreground="{DynamicResource MutedForegroundBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="{Binding HasNoSelectedServer, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>