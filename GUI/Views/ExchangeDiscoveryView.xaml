<UserControl x:Class="MandADiscoverySuite.Views.ExchangeDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="ExchangeDiscoveryControl"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

        <UserControl.Resources>
         <ResourceDictionary>
             <ResourceDictionary.MergedDictionaries>
                 <ResourceDictionary Source="../Resources/Converters.xaml" />
                 <ResourceDictionary Source="../Resources/DataGridTheme.xaml" />
                 <ResourceDictionary Source="../Themes/ThemeStyles.xaml" />
                 <ResourceDictionary Source="../Themes/Colors.xaml" />
             </ResourceDictionary.MergedDictionaries>
         </ResourceDictionary>
     </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Header Section -->
        <TextBlock Grid.Row="0" Text="Exchange Discovery" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Total Mailboxes Card -->
            <Border Background="#FFE6F3FF" BorderBrush="#FF0066CC" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Total Mailboxes" FontWeight="Bold" FontSize="14" Foreground="#FF0066CC"/>
                    <TextBlock Text="📧" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalMailboxes}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#FF0066CC"/>
                </StackPanel>
            </Border>
            <!-- Shared Mailboxes Card -->
            <Border Background="#FFF0F9FF" BorderBrush="#FF0891B2" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Shared Mailboxes" FontWeight="Bold" FontSize="14" Foreground="#FF0891B2"/>
                    <TextBlock Text="📬" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalSharedMailboxes}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#FF0891B2"/>
                </StackPanel>
            </Border>
            <!-- Distribution Groups Card -->
            <Border Background="#FFE0F2FE" BorderBrush="#FF0277BD" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Distribution Groups" FontWeight="Bold" FontSize="14" Foreground="#FF0277BD"/>
                    <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalDistributionGroups}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#FF0277BD"/>
                </StackPanel>
            </Border>
            <!-- Mail-Enabled Security Groups Card -->
            <Border Background="#FFE8F5E8" BorderBrush="#FF2E7D32" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Mail-Enabled Security Groups" FontWeight="Bold" FontSize="14" Foreground="#FF2E7D32"/>
                    <TextBlock Text="🔒" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalMailEnabledSecurityGroups}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#FF2E7D32"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Run Discovery" Command="{Binding RunDiscoveryCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Refresh" Command="{Binding RefreshDataCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
        </ToolBar>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- Red Warning Banners for Missing CSV Headers -->
            <ItemsControl Grid.Row="0" Grid.ColumnSpan="3" ItemsSource="{Binding HeaderWarnings}"
                          Margin="10,0,10,10"
                          Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                                CornerRadius="4" Margin="0,0,0,8" Padding="8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Warning:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Error Banner -->
            <Border Grid.Row="1" Grid.ColumnSpan="3" Background="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                    CornerRadius="4" Margin="10,0,10,10" Padding="8" Opacity="0.33"
                    Visibility="{Binding HasErrors, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Error:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Loading Spinner -->
            <Border Grid.Row="2" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="10"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"
                                 VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ProcessingMessage}" Margin="10,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="3" Grid.Column="0" ItemsSource="{Binding SelectedResults}"
                      Margin="10"
                      SelectedItem="{Binding SelectedItem}"
                      Visibility="{Binding SelectedResults.Count, Converter={StaticResource CountToVisibility}}"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Extended" GridLinesVisibility="Horizontal"
                      AutoGenerateColumns="False"
                      Background="Transparent">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mailbox Name" Binding="{Binding mailboxname}" Width="*" />
                    <DataGridTextColumn Header="Primary SMTP" Binding="{Binding primarysmtpaddress}" Width="200" />
                    <DataGridTextColumn Header="Mailbox Type" Binding="{Binding mailboxtype}" Width="150" />
                    <DataGridTextColumn Header="Size (GB)" Binding="{Binding sizegb}" Width="100" />
                    <DataGridTextColumn Header="Archive Status" Binding="{Binding archivestatus}" Width="120" />
                    <DataGridTextColumn Header="Last Login" Binding="{Binding lastlogin, StringFormat='{}{0:g}'}" Width="120" />
                    <DataGridTextColumn Header="Litigation Hold" Binding="{Binding litigationholdenabled}" Width="120" />
                    <DataGridTemplateColumn Header="Actions" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View Details"
                                        FontSize="11"
                                        Padding="8,4"
                                        Background="#FF10B981"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4"
                                                                BorderThickness="0">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#FF059669"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- No Data Message -->
            <Border Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding SelectedResults.Count, Converter={StaticResource InverseCountToVisibility}}">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <TextBlock Text="No Exchange Data Found" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="No Exchange data found" FontSize="14" HorizontalAlignment="Center"
                               Margin="0,10,0,0" Opacity="0.7"/>
                    <TextBlock Text="Check CSV files in data directory" FontSize="12" HorizontalAlignment="Center"
                               Margin="0,5,0,0" Opacity="0.5"/>
                </StackPanel>
            </Border>

            <!-- GridSplitter for Details Panel -->
            <GridSplitter Grid.Row="3" Grid.Column="1" Width="5" Background="{DynamicResource BorderBrush}"/>

            <!-- Details Panel -->
            <Border Grid.Row="3" Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Margin="0,10,10,10"
                    Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Mailbox Details" FontSize="18" FontWeight="Bold" Margin="15,15,15,10"/>
                        <ItemsControl ItemsSource="{Binding SelectedItemDetails}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="15,0,15,10">
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding Value}" Foreground="{DynamicResource MutedForegroundBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="#FF1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Exchange Discovery - Ready for use" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF4A5568"/>
        </Border>
    </Grid>
</UserControl>
