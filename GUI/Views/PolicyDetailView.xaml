<UserControl x:Class="MandADiscoverySuite.Views.PolicyDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="10" Orientation="Vertical">
            <TextBlock Text="Group Policy" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Name:" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="{Binding Policy.Name}"/>
                <TextBlock Grid.Row="1" Text="Path:" FontWeight="Bold"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Policy.Path}"/>
                <TextBlock Grid.Row="2" Text="Scope:" FontWeight="Bold"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Policy.Scope}"/>
                <TextBlock Grid.Row="3" Text="Linked OUs:" FontWeight="Bold"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Policy.LinkedOUs}"/>
            </Grid>
            <TabControl>
                <TabItem Header="Users">
                    <DataGrid ItemsSource="{Binding AffectedUsers}" AutoGenerateColumns="True"
                              VirtualizingStackPanel.IsVirtualizing="True"/>
                </TabItem>
                <TabItem Header="Computers">
                    <DataGrid ItemsSource="{Binding AffectedComputers}" AutoGenerateColumns="True"
                              VirtualizingStackPanel.IsVirtualizing="True"/>
                </TabItem>
                <TabItem Header="Groups">
                    <DataGrid ItemsSource="{Binding AffectedGroups}" AutoGenerateColumns="True"
                              VirtualizingStackPanel.IsVirtualizing="True"/>
                </TabItem>
                <TabItem Header="Applications">
                    <DataGrid ItemsSource="{Binding AffectedApplications}" AutoGenerateColumns="True"
                              VirtualizingStackPanel.IsVirtualizing="True"/>
                </TabItem>
            </TabControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
