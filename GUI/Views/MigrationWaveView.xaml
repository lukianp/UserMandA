<Window x:Class="MandADiscoverySuite.Views.MigrationWaveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Migration Waves" Height="400" Width="600">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding MigrationWaves}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Start" Binding="{Binding PlannedStartDate}" Width="120"/>
                <DataGridTextColumn Header="End" Binding="{Binding PlannedEndDate}" Width="120"/>
                <DataGridTextColumn Header="Users" Binding="{Binding AssignedUserCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
