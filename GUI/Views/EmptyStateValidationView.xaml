<UserControl x:Class="MandADiscoverySuite.Views.EmptyStateValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MandADiscoverySuite.Views"
             xmlns:viewmodels="clr-namespace:MandADiscoverySuite.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Empty State Validation"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="🔍 Run Validation"
                    Command="{Binding RunValidationCommand}"
                    IsEnabled="{Binding IsValidating, Converter={StaticResource InverseBooleanConverter}}"/>
            <Button Content="🧹 Clear Results"
                    Command="{Binding ClearResultsCommand}"/>
            <TextBlock Text="{Binding ValidationStatus}"
                       VerticalAlignment="Center" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Summary -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Last Run:" FontWeight="Bold"/>
            <TextBlock Grid.Column="1" Text="{Binding LastValidationTime, StringFormat='{}{0:mm\\:ss\\.fff}'}"
                       Margin="10,0,0,0"/>

            <TextBlock Grid.Column="2" Text="Total:" FontWeight="Bold" Margin="20,0,0,0"/>
            <TextBlock Grid.Column="3" Text="{Binding TotalTests}" Margin="10,0,0,0"/>

            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="✅ Passed:" FontWeight="Bold"/>
                <TextBlock Text="{Binding PassedTests}" Margin="5,0,0,0" Foreground="Green"/>
                <TextBlock Text="❌ Failed:" FontWeight="Bold" Margin="15,0,0,0"/>
                <TextBlock Text="{Binding FailedTests}" Margin="5,0,0,0" Foreground="Red"/>
            </StackPanel>
        </Grid>

        <!-- Results -->
        <GroupBox Grid.Row="3" Header="Validation Results" Margin="0,20,0,0">
            <DataGrid ItemsSource="{Binding ValidationResults}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      Height="400" VirtualizingStackPanel.IsVirtualizing="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ViewModel" Binding="{Binding ViewModelName}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground">
                                    <Setter.Value>
                                        <Binding Path="IsSuccessful" Converter="{StaticResource BooleanToColorConverter}">
                                            <Binding.ConverterParameter>
                                                <x:Array Type="SolidColorBrush">
                                                    <SolidColorBrush Color="Green"/>
                                                    <SolidColorBrush Color="Red"/>
                                                </x:Array>
                                            </Binding.ConverterParameter>
                                        </Binding>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Time (ms)" Binding="{Binding ValidationTime.TotalMilliseconds, StringFormat='{}{0:F0}'}" Width="80"/>
                    <DataGridTextColumn Header="Issues" Binding="{Binding IssuesCount}" Width="60"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding Issues}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>