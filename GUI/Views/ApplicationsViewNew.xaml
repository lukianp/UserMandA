<UserControl x:Class="MandADiscoverySuite.Views.ApplicationsViewNew"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Converters.xaml"/>
                <ResourceDictionary Source="../Resources/DataGridTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Warning Banners -->
            <RowDefinition Height="Auto"/>  <!-- Error Banner -->
            <RowDefinition Height="Auto"/>  <!-- Loading -->
            <RowDefinition Height="*"/>     <!-- Data -->
        </Grid.RowDefinitions>
        
        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Applications" FontSize="24" FontWeight="Bold" 
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Applications.Count, StringFormat='({0} items)'}" 
                       FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding HasData, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
        
        <!-- Red Warning Banners for Missing CSV Headers -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding HeaderWarnings}" 
                      Margin="10,0,10,10"
                      Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                            CornerRadius="4" Margin="0,0,0,8" Padding="8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠" FontSize="16" Foreground="{DynamicResource WhiteBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <!-- Error Banner -->
        <Border Grid.Row="2" Background="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                CornerRadius="4" Margin="10,0,10,10" Padding="8" Opacity="0.33"
                Visibility="{Binding LastError, Converter={StaticResource NullToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="❌" FontSize="16" Foreground="{DynamicResource WhiteBrush}"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding LastError}" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Border>
        
        <!-- Loading Spinner -->
        <Border Grid.Row="3" HorizontalAlignment="Center" Margin="10"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" 
                             VerticalAlignment="Center"/>
                <TextBlock Text="Loading Applications..." Margin="10,0,0,0" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Data Grid -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding Applications}" 
                  AutoGenerateColumns="False" Margin="10"
                  Visibility="{Binding HasData, Converter={StaticResource BoolToVisibility}}"
                  CanUserAddRows="False" CanUserDeleteRows="False" 
                  SelectionMode="Extended" GridLinesVisibility="Horizontal">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120"/>
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="150"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="User Count" Binding="{Binding UserCount}" Width="80"/>
                <DataGridTextColumn Header="Group Count" Binding="{Binding GroupCount}" Width="80"/>
                <DataGridTextColumn Header="Device Count" Binding="{Binding DeviceCount}" Width="80"/>
                <DataGridTextColumn Header="Last Seen" Binding="{Binding LastSeen, StringFormat=yyyy-MM-dd}" Width="100"/>
            </DataGrid.Columns>
            
        </DataGrid>
        
        <!-- No Data Message -->
        <Border Grid.Row="4" HorizontalAlignment="Center" VerticalAlignment="Center"
                Visibility="{Binding HasData, Converter={StaticResource InverseBoolToVisibility}}">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <TextBlock Text="📱" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                <TextBlock Text="No Applications data found" FontSize="16" HorizontalAlignment="Center" 
                           Margin="0,10,0,0" Opacity="0.7"/>
                <TextBlock Text="Check CSV files in data directory" FontSize="12" HorizontalAlignment="Center" 
                           Margin="0,5,0,0" Opacity="0.5"/>
            </StackPanel>
        </Border>
        
    </Grid>
</UserControl>
