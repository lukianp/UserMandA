<UserControl x:Class="MandADiscoverySuite.Views.ExchangeMigrationPlanningViewSimple"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MandADiscoverySuite.ViewModels;assembly=MandADiscoverySuite"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200"
             DataContext="{Binding ., Source={StaticResource ViewModel}}"
             Loaded="View_Loaded"
             Unloaded="View_Unloaded">

    <UserControl.Resources>
        <vm:ExchangeMigrationPlanningViewModelSimple x:Key="ViewModel" d:IsDataSource="True"/>
    </UserControl.Resources>

    <Grid>
        <!-- Basic layout placeholder -->
        <TextBlock Text="Exchange Migration Planning View - Simple"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="18"/>
    </Grid>
</UserControl>
