<UserControl x:Class="MandADiscoverySuite.Views.ExchangeMigrationPlanningViewSimple"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:MandADiscoverySuite.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#FF0F1419">
    
    <UserControl.DataContext>
        <viewmodels:ExchangeMigrationPlanningViewModelSimple />
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#2C3E50" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
        
        <Style x:Key="StatisticCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA" />
            <Setter Property="BorderBrush" Value="#E9ECEF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="Margin" Value="5" />
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="FontWeight" Value="Medium" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Exchange Migration Planning" 
                             FontSize="24" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="{Binding StatusMessage}" 
                             FontSize="14" Foreground="#BDC3C7" Margin="0,5,0,0" />
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,0" VerticalAlignment="Center">
                        <TextBlock Text="Target Profile:" Foreground="#BDC3C7" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <ComboBox Width="260" ItemsSource="{Binding TargetProfiles}" SelectedItem="{Binding SelectedTargetProfile}"
                                  DisplayMemberPath="Name"/>
                    </StackPanel>
                    <Button Content="Import Mailboxes" Command="{Binding ImportMailboxesCommand}" 
                          Style="{StaticResource ActionButtonStyle}" />
                    <Button Content="Export Plan" Command="{Binding ExportPlanCommand}" 
                          Style="{StaticResource ActionButtonStyle}" Background="#E67E22" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <WrapPanel Grid.Row="1" Margin="10" HorizontalAlignment="Center">
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalMailboxes}" FontSize="28" FontWeight="Bold" 
                             Foreground="#3498DB" HorizontalAlignment="Center" />
                    <TextBlock Text="Total Mailboxes" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding ValidatedMailboxes}" FontSize="28" FontWeight="Bold" 
                             Foreground="#27AE60" HorizontalAlignment="Center" />
                    <TextBlock Text="Validated" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding MailboxesWithIssues}" FontSize="28" FontWeight="Bold" 
                             Foreground="#E74C3C" HorizontalAlignment="Center" />
                    <TextBlock Text="With Issues" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalDataSizeGB, StringFormat='{}{0:N0} GB'}" 
                             FontSize="28" FontWeight="Bold" 
                             Foreground="#F39C12" HorizontalAlignment="Center" />
                    <TextBlock Text="Est. Data Size" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalWaves}" FontSize="28" FontWeight="Bold" 
                             Foreground="#9B59B6" HorizontalAlignment="Center" />
                    <TextBlock Text="Migration Waves" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatisticCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding EstimatedMigrationDays}" FontSize="28" FontWeight="Bold" 
                             Foreground="#1ABC9C" HorizontalAlignment="Center" />
                    <TextBlock Text="Est. Days" FontSize="12" Foreground="#7F8C8D" 
                             HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- Main Content Tabs -->
        <TabControl Grid.Row="2" Margin="10" Background="White">
            
            <!-- Project Settings Tab -->
            <TabItem Header="Project Settings" FontSize="14" FontWeight="Medium">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <!-- Left Column -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Basic Information" Style="{StaticResource HeaderTextStyle}" />
                            
                            <TextBlock Text="Project Name:" FontWeight="Medium" Margin="0,0,0,5" />
                            <TextBox Text="{Binding ProjectName}" Margin="0,0,0,15" Padding="8" />
                            
                            <TextBlock Text="Description:" FontWeight="Medium" Margin="0,0,0,5" />
                            <TextBox Text="{Binding ProjectDescription}" Margin="0,0,0,15" Padding="8" 
                                   TextWrapping="Wrap" Height="60" AcceptsReturn="True" />
                            
                            <TextBlock Text="Source Environment:" FontWeight="Medium" Margin="0,0,0,5" />
                            <TextBox Text="{Binding SourceEnvironment}" Margin="0,0,0,15" Padding="8" />
                            
                            <TextBlock Text="Target Environment:" FontWeight="Medium" Margin="0,0,0,5" />
                            <TextBox Text="{Binding TargetEnvironment}" Margin="0,0,0,15" Padding="8" />
                        </StackPanel>
                        
                        <!-- Right Column -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Migration Options" Style="{StaticResource HeaderTextStyle}" />
                            
                            <CheckBox Content="Preserve Permissions" IsChecked="{Binding PreservePermissions}" 
                                    Margin="0,0,0,10" />
                            <CheckBox Content="Migrate Archives" IsChecked="{Binding MigrateArchives}" 
                                    Margin="0,0,0,10" />
                            <CheckBox Content="Enable Delta Sync" IsChecked="{Binding EnableDeltaSync}" 
                                    Margin="0,0,0,15" />
                            
                            <TextBlock Text="Max Concurrent Migrations:" FontWeight="Medium" Margin="0,0,0,5" />
                            <TextBox Text="{Binding MaxConcurrentMigrations}" Margin="0,0,0,15" Padding="8" />
                            
                            <TextBlock Text="Planned Start Date:" FontWeight="Medium" Margin="0,0,0,5" />
                            <DatePicker SelectedDate="{Binding PlannedStartDate}" Margin="0,0,0,15" />
                            
                            <TextBlock Text="Planned End Date:" FontWeight="Medium" Margin="0,0,0,5" />
                            <DatePicker SelectedDate="{Binding PlannedEndDate}" Margin="0,0,0,15" />
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
            
            <!-- Mailboxes Tab -->
            <TabItem Header="Mailboxes" FontSize="14" FontWeight="Medium">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <!-- Actions and Filters -->
                    <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E9ECEF" 
                          BorderThickness="0,0,0,1" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- Filters -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBox Text="{Binding MailboxSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                       Width="200" Margin="0,0,10,0" Padding="8" />
                                
                                <CheckBox Content="Show only issues" IsChecked="{Binding ShowOnlyIssues}" 
                                        VerticalAlignment="Center" Margin="0,0,10,0" />
                                
                                <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" 
                                      Style="{StaticResource ActionButtonStyle}" Background="#95A5A6" />
                            </StackPanel>
                            
                            <!-- Actions -->
                            <Button Grid.Column="1" Content="Validate All" Command="{Binding ValidateMailboxesCommand}" 
                                  Style="{StaticResource ActionButtonStyle}" Background="#27AE60" />
                        </Grid>
                    </Border>
                    
                    <!-- Validation Progress -->
                    <ProgressBar Grid.Row="1" Height="4" Minimum="0" Maximum="100" 
                               Value="{Binding ValidationProgress}" 
                               Visibility="{Binding IsValidationRunning, Converter={StaticResource BooleanToVisibilityConverter}}"
                               VerticalAlignment="Top" />
                    
                    <!-- Mailboxes DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding MailboxesView}" 
                            SelectedItem="{Binding SelectedMailbox}"
                            AutoGenerateColumns="False" CanUserAddRows="False" 
                            GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E9ECEF"
                            RowBackground="White" AlternatingRowBackground="#F8F9FA"
                            Margin="0,4,0,0">
                        
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="View Details" Command="{Binding ViewMailboxDetailsCommand}"
                                        CommandParameter="{Binding SelectedMailbox}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Source Identity" Binding="{Binding SourceIdentity}" 
                                              Width="200" />
                            <DataGridTextColumn Header="Target Identity" Binding="{Binding TargetIdentity}" 
                                              Width="250" />
                            <DataGridTextColumn Header="Migration Type" Binding="{Binding Type}" 
                                              Width="120" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" 
                                              Width="120" />
                            <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" 
                                              Width="80" />
                            <DataGridTextColumn Header="Start Time" 
                                              Binding="{Binding StartTime, StringFormat='yyyy-MM-dd'}" 
                                              Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- Migration Waves Tab -->
            <TabItem Header="Migration Waves" FontSize="14" FontWeight="Medium">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <!-- Wave Actions -->
                    <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E9ECEF" 
                          BorderThickness="0,0,0,1" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" Text="{Binding WaveSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                   Width="300" Padding="8" HorizontalAlignment="Left" />
                            
                            <Button Grid.Column="1" Content="Generate Waves" Command="{Binding GenerateWavesCommand}" 
                                  Style="{StaticResource ActionButtonStyle}" Background="#9B59B6" />
                        </Grid>
                    </Border>
                    
                    <!-- Waves DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding WavesView}" 
                            SelectedItem="{Binding SelectedWave}"
                            AutoGenerateColumns="False" CanUserAddRows="False" 
                            GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E9ECEF"
                            RowBackground="White" AlternatingRowBackground="#F8F9FA">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Wave Name" Binding="{Binding Name}" 
                                              Width="200" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" 
                                              Width="300" />
                            <DataGridTextColumn Header="Mailbox Count" Binding="{Binding Items.Count}" 
                                              Width="100" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" 
                                              Width="100" />
                            <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" 
                                              Width="80" />
                            <DataGridTextColumn Header="Scheduled Start" 
                                              Binding="{Binding ScheduledStartTime, StringFormat='yyyy-MM-dd HH:mm'}" 
                                              Width="140" />
                            <DataGridTextColumn Header="Est. Duration (hrs)" Binding="{Binding EstimatedDurationHours}" 
                                              Width="120" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <!-- Validation Issues Tab -->
            <TabItem Header="Validation Issues" FontSize="14" FontWeight="Medium">
                <DataGrid ItemsSource="{Binding ValidationIssues}" 
                        AutoGenerateColumns="False" CanUserAddRows="False" 
                        GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E9ECEF"
                        RowBackground="White" AlternatingRowBackground="#F8F9FA">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" 
                                          Width="100" />
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" 
                                          Width="150" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" 
                                          Width="400" />
                        <DataGridTextColumn Header="Recommended Action" Binding="{Binding RecommendedAction}" 
                                          Width="300" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#ECF0F1" BorderBrush="#BDC3C7" 
              BorderThickness="0,1,0,0" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                         FontSize="12" Foreground="#34495E" VerticalAlignment="Center" />
                
                <Button Grid.Column="1" Content="Refresh Statistics" Command="{Binding RefreshStatisticsCommand}" 
                      Style="{StaticResource ActionButtonStyle}" Background="#7F8C8D" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
