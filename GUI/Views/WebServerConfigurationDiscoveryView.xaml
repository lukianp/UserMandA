<UserControl x:Class="MandADiscoverySuite.Views.WebServerConfigurationDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Header Section -->
        <TextBlock Grid.Row="0" Text="Web Server Configuration Discovery" Style="{StaticResource HeaderTextStyle}" FontSize="20"/>
        <!-- Placeholder: Replace with customized header content in each view -->

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Web Servers Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="???" FontSize="20" Margin="0,0,8,0"/>
                        <TextBlock Text="Web Servers" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                    <TextBlock Text="{Binding WebServersCount}" FontSize="24" FontWeight="SemiBold" Foreground="#2B6CB0"/>
                </StackPanel>
            </Border>
            <!-- IIS Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="??" FontSize="20" Margin="0,0,8,0"/>
                        <TextBlock Text="IIS" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                    <TextBlock Text="{Binding IISCount}" FontSize="24" FontWeight="SemiBold" Foreground="#3182CE"/>
                </StackPanel>
            </Border>
            <!-- Apache Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="??" FontSize="20" Margin="0,0,8,0"/>
                        <TextBlock Text="Apache" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ApacheCount}" FontSize="24" FontWeight="SemiBold" Foreground="#D69E2E"/>
                </StackPanel>
            </Border>
            <!-- Total Websites Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="??" FontSize="20" Margin="0,0,8,0"/>
                        <TextBlock Text="Total Websites" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalWebsites}" FontSize="24" FontWeight="SemiBold" Foreground="#38A169"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Refresh" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Clear Filters" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <!-- Placeholder: Add more buttons as needed in each view -->
        </ToolBar>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid Placeholder -->
            <DataGrid Grid.Column="0" ItemsSource="{Binding SelectedResults}"
                      AutoGenerateColumns="False" Margin="10"
                      SelectedItem="{Binding SelectedItem}"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Single" GridLinesVisibility="Horizontal">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Server Name" Binding="{Binding Name}" Width="200" />
                    <DataGridTextColumn Header="Type" Binding="{Binding FrameworkName}" Width="150" />
                    <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100" />
                    <DataGridTextColumn Header="Websites" Binding="{Binding WebsitesCount}" Width="100" />
                    <DataGridTextColumn Header="App Pools" Binding="{Binding AppPoolsCount}" Width="100" />

                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Actions" Width="120" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View Details"
                                        FontSize="11"
                                        Padding="8,4"
                                        Background="#3182CE"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4"
                                                                BorderThickness="0">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#2C5282"/>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="Background" Value="#2A4365"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel for Website Configuration and Security Settings -->
            <Border Grid.Column="2" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Website Configuration &amp; Security" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#E2E8F0"/>

                        <!-- Configuration Section -->
                        <TextBlock Text="Configuration Settings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#CBD5E0"/>
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Server Type:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.FrameworkName}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Version:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.Version}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Installation Path:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.PhysicalPath, FallbackValue={Binding SelectedItem.InstallPath}}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Websites Count:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.WebsitesCount}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="App Pools Count:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.AppPoolsCount}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- Security Section -->
                        <TextBlock Text="Security Settings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#CBD5E0"/>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="SSL/TLS Status:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.SSLStatus, FallbackValue='Not configured'}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Authentication:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.AuthenticationMethods, FallbackValue='Not specified'}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Enabled Protocols:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.EnabledProtocols, FallbackValue='Not specified'}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Bindings:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.Bindings, FallbackValue='Not configured'}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Last Security Scan:" FontWeight="SemiBold" FontSize="14" Foreground="#CBD5E0"/>
                            <TextBlock Text="{Binding SelectedItem.LastSecurityScan, StringFormat=g, FallbackValue='Not performed'}" FontSize="14" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Web Server Configuration Discovery - Ready" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
        </Border>
    </Grid>
</UserControl>
