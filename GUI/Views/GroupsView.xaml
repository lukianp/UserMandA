<UserControl x:Class="MandADiscoverySuite.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#FF0F1419">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Security Groups" 
                       FontSize="28" 
                       FontWeight="SemiBold"
                       Foreground="#FFE2E8F0"/>
            <TextBlock Text="Manage and view security group information" 
                       FontSize="14" 
                       Foreground="#FFA0AEC0" 
                       Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Toolbar -->
        <Border Grid.Row="1" 
                Background="#FF1A202C" 
                BorderBrush="#FF2D3748" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="16,12" 
                Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="300" Height="36" 
                         Background="#FF2D3748" 
                         Foreground="#FFE2E8F0" 
                         BorderBrush="#FF4A5568" 
                         BorderThickness="1"
                         VerticalContentAlignment="Center"
                         Padding="12,0"
                         FontSize="14"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <Grid>
                                                <TextBlock Text="Search groups..." 
                                                           Foreground="#FF6B7280" 
                                                           FontSize="14"
                                                           Margin="12,0"
                                                           VerticalAlignment="Center"
                                                           Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                <ScrollViewer x:Name="PART_ContentHost"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                
                <Button Content="🔄" 
                        Width="36" Height="36" 
                        Margin="12,0,0,0"
                        Background="#FF4299E1" 
                        BorderThickness="0" 
                        Foreground="White"
                        FontSize="14"
                        Command="{Binding RefreshGroupsCommand}"
                        ToolTip="Refresh Groups"
                        Style="{StaticResource {x:Type Button}}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Row="2" 
                Background="#FF1A202C" 
                BorderBrush="#FF2D3748" 
                BorderThickness="1" 
                CornerRadius="8">
            <Grid>
                <!-- Loading Indicator -->
                <StackPanel HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="{Binding LoadingMessage}" 
                               FontSize="16" 
                               Foreground="#FFA0AEC0" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
                
                <!-- Data Grid -->
                <DataGrid ItemsSource="{Binding Groups}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Margin="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Group Name" Binding="{Binding DisplayName}" Width="200" />
                        <DataGridTextColumn Header="Group Type" Binding="{Binding GroupType}" Width="150" />
                        <DataGridCheckBoxColumn Header="Mail Enabled" Binding="{Binding MailEnabled}" Width="80" />
                        <DataGridCheckBoxColumn Header="Security Enabled" Binding="{Binding SecurityEnabled}" Width="80" />
                        <DataGridTextColumn Header="Mail" Binding="{Binding Mail}" Width="220" />
                        <DataGridTextColumn Header="Created" Binding="{Binding CreatedDateTime, StringFormat=yyyy-MM-dd}" Width="120" />
                        <DataGridTextColumn Header="Members" Binding="{Binding MemberCount}" Width="80" />
                        <DataGridTextColumn Header="Owners" Binding="{Binding OwnerCount}" Width="80" />
                        <DataGridTextColumn Header="Visibility" Binding="{Binding Visibility}" Width="100" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                    <DataGrid.Resources>
                        <!-- DataGrid Header Style -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FF2D3748"/>
                            <Setter Property="Foreground" Value="#FFE2E8F0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="#FF4A5568"/>
                        </Style>

                        <!-- DataGrid Row Style -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#FFE2E8F0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF2D3748"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FF4299E1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <!-- DataGrid Cell Style -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>
                
                <!-- Empty State -->
                <StackPanel HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="Collapsed">
                    <TextBlock Text="👥" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="#FF6B7280"/>
                    <TextBlock Text="No groups found" 
                               FontSize="18" 
                               Foreground="#FF6B7280" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Security groups will appear here once discovery is run" 
                               FontSize="14" 
                               Foreground="#FF4B5563" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>