<UserControl x:Class="MandADiscoverySuite.Views.ManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#1A202C">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Modern Card Style -->
            <Style x:Key="ModernCardStyle" TargetType="Border">
                <Setter Property="Background" Value="#2D3748"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="24"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.1" ShadowDepth="8" Direction="270"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Header Style -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="28"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#F7FAFC"/>
            </Style>

            <!-- Section Header Style -->
            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#E2E8F0"/>
                <Setter Property="Margin" Value="0,0,0,16"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <StackPanel Grid.Row="0" Margin="0,0,0,32">
                <TextBlock Text="Management" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Configure and manage system settings and profiles" 
                           Foreground="{StaticResource SecondaryTextBrush}" FontSize="16" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Management Content -->
            <Border Grid.Row="1" Style="{StaticResource ModernCardStyle}">
                <StackPanel>
                    <TextBlock Text="System Management" Style="{StaticResource SectionHeaderStyle}"/>
                    
                    <!-- Placeholder content - will be expanded based on ManagementViewModel functionality -->
                    <TextBlock Text="Management features will be implemented here" 
                               Foreground="{StaticResource SecondaryTextBrush}" FontSize="14"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>