<UserControl x:Class="MandADiscoverySuite.Views.MicrosoftTeamsDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

        <UserControl.Resources>
                  <ResourceDictionary>
                       <!-- Temporarily removed resource dictionaries to isolate SolidColorBrush.Color parsing error -->
                   </ResourceDictionary>
               </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section with Icon, Title, and Total Records -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Image Source="{Binding ModuleIcon}" Width="32" Height="32" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ModuleTitle}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalTeams, StringFormat=' ({0} records)'}" FontSize="16" Foreground="Gray" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Run Discovery" Command="{Binding RunDiscoveryCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Refresh Data" Command="{Binding RefreshDataCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
        </ToolBar>

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Total Teams Card -->
            <Border Background="#E6F3FF" BorderBrush="#0066CC" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Total Teams" FontWeight="Bold" FontSize="14" Foreground="#0066CC"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalTeams}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0066CC"/>
                </StackPanel>
            </Border>
            <!-- Active Channels Card -->
            <Border Background="#F0F9FF" BorderBrush="#0891B2" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Active Channels" FontWeight="Bold" FontSize="14" Foreground="#0891B2"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalChannels}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0891B2"/>
                </StackPanel>
            </Border>
            <!-- Total Members Card -->
            <Border Background="#E0F2FE" BorderBrush="#0277BD" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Total Members" FontWeight="Bold" FontSize="14" Foreground="#0277BD"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalMembers}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#0277BD"/>
                </StackPanel>
            </Border>
            <!-- Guest Users Card -->
            <Border Background="#E8F5E8" BorderBrush="#2E7D32" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Guest Users" FontWeight="Bold" FontSize="14" Foreground="#2E7D32"/>
                    <TextBlock Text="??" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,8"/>
                    <TextBlock Text="{Binding TotalGuests}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#2E7D32"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Red Warning Banners for Missing CSV Headers -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding HeaderWarnings}"
                      Margin="10,0,10,10"
                      Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                            CornerRadius="4" Margin="0,0,0,8" Padding="8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Warning:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Error Banner -->
        <Border Grid.Row="4" Background="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                CornerRadius="4" Margin="10,0,10,10" Padding="8" Opacity="0.33"
                Visibility="{Binding HasErrors, Converter={StaticResource BoolToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Error:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Loading Spinner -->
        <Border Grid.Row="5" HorizontalAlignment="Center" Margin="10"
                Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"
                             VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ProcessingMessage}" Margin="10,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- Red Warning Banners for Missing CSV Headers -->
            <ItemsControl Grid.Row="0" Grid.ColumnSpan="3" ItemsSource="{Binding HeaderWarnings}"
                          Margin="10,0,10,10"
                          Visibility="{Binding HeaderWarnings.Count, Converter={StaticResource CountToVisibility}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource ErrorBrush}" Opacity="0.33" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                                CornerRadius="4" Margin="0,0,0,8" Padding="8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Warning:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Error Banner -->
            <Border Grid.Row="1" Grid.ColumnSpan="3" Background="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="1"
                    CornerRadius="4" Margin="10,0,10,10" Padding="8" Opacity="0.33"
                    Visibility="{Binding HasErrors, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Error:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource ErrorForegroundBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Foreground="{DynamicResource ErrorForegroundBrush}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Loading Spinner -->
            <Border Grid.Row="2" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="10"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"
                                 VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ProcessingMessage}" Margin="10,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="3" Grid.Column="0" ItemsSource="{Binding SelectedResults}"
                      Margin="10"
                      SelectedItem="{Binding SelectedItem}"
                      Visibility="{Binding SelectedResults.Count, Converter={StaticResource CountToVisibility}}"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Extended" GridLinesVisibility="Horizontal"
                      AutoGenerateColumns="False"
                      Background="Transparent">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Team Name" Binding="{Binding teamname}" Width="*" />
                    <DataGridTextColumn Header="Visibility" Binding="{Binding visibility}" Width="100" />
                    <DataGridTextColumn Header="Member Count" Binding="{Binding membercount}" Width="100" />
                    <DataGridTextColumn Header="Channel Count" Binding="{Binding channelcount}" Width="100" />
                    <DataGridTextColumn Header="Last Activity" Binding="{Binding lastactivitydate, StringFormat=d}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- No Data Message -->
            <Border Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding SelectedResults.Count, Converter={StaticResource InverseCountToVisibility}}">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <TextBlock Text="No Teams Data Found" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="No Teams data found" FontSize="14" HorizontalAlignment="Center"
                               Margin="0,10,0,0" Opacity="0.7"/>
                    <TextBlock Text="Check CSV files in data directory" FontSize="12" HorizontalAlignment="Center"
                               Margin="0,5,0,0" Opacity="0.5"/>
                </StackPanel>
            </Border>

            <!-- GridSplitter for Details Panel -->
            <GridSplitter Grid.Row="3" Grid.Column="1" Width="5" Background="{DynamicResource BorderBrush}"/>

            <!-- Details Panel -->
            <Border Grid.Row="3" Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Margin="0,10,10,10"
                    Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Team Details" FontSize="18" FontWeight="Bold" Margin="15,15,15,10"/>
                        <ItemsControl ItemsSource="{Binding SelectedItemDetails}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="15,0,15,10">
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding Value}" Foreground="{DynamicResource MutedForegroundBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Microsoft Teams Discovery - Ready for use" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
        </Border>
    </Grid>
</UserControl>
