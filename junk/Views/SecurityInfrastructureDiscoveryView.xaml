<UserControl x:Class="MandADiscoverySuite.Views.SecurityInfrastructureDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Header Section -->
        <TextBlock Grid.Row="0" Text="?? Security Infrastructure Discovery" Style="{StaticResource HeaderTextStyle}" FontSize="24"/>
        <!-- Placeholder: Replace with customized header content in each view -->

        <!-- UniformGrid for 4 Summary Statistic Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <!-- Total Devices Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Total Devices" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding TotalDevices}" FontSize="24" FontWeight="SemiBold" Foreground="#3182CE"/>
                </StackPanel>
            </Border>
            <!-- VPNs Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="VPNs" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding TotalVpns}" FontSize="24" FontWeight="SemiBold" Foreground="#38A169"/>
                </StackPanel>
            </Border>
            <!-- Firewalls Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Firewalls" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding TotalFirewalls}" FontSize="24" FontWeight="SemiBold" Foreground="#D69E2E"/>
                </StackPanel>
            </Border>
            <!-- IDS/IPS Card -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="IDS/IPS" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding TotalIdsIps}" FontSize="24" FontWeight="SemiBold" Foreground="#E53E3E"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- ToolBar for Action Buttons -->
        <ToolBar Grid.Row="2" Margin="0,0,0,16">
            <Button Content="Refresh" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Export" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Clear Filters" Style="{StaticResource RoundedButtonStyle}" Padding="12,6" Margin="0,0,8,0"/>
            <!-- Placeholder: Add more buttons as needed in each view -->
        </ToolBar>

        <!-- Grid with GridSplitter to separate DataGrid from Details Panel -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Main DataGrid -->
            <Border Grid.Column="0" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1">
                <DataGrid ItemsSource="{Binding SelectedResults}" AutoGenerateColumns="False" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Device Name" Binding="{Binding DeviceName}" Width="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                        <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="150"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                        <DataGridTextColumn Header="Firmware Version" Binding="{Binding FirmwareVersion}" Width="150"/>
                        <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

            <!-- Right-hand Details Panel -->
            <Border Grid.Column="2" Background="#1A202C" BorderBrush="#E2E8F0" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Device Configuration &amp; Security Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                        <StackPanel Visibility="{Binding SelectedItem, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Device Details:" FontWeight="Bold" Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding SelectedItemDetails}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,10">
                                            <TextBlock Text="{Binding Key}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Value}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="Security Configuration:" FontWeight="Bold" Margin="0,16,0,8"/>
                            <TextBlock Text="{Binding SelectedItemSecurityConfig, FallbackValue='No security configuration available'}" TextWrapping="Wrap"/>
                        </StackPanel>
                        <TextBlock Text="Select a device to view details" FontStyle="Italic" Visibility="{Binding SelectedItem, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer or Empty State Placeholder -->
        <Border Grid.Row="4" Background="#1A202C" Padding="16" Margin="0,16,0,0">
            <TextBlock Text="Security Infrastructure Metrics &amp; Status" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#4A5568"/>
            <!-- Placeholder: Replace with footer or empty state content in each view -->
        </Border>
    </Grid>
</UserControl>